<ul class="nav justify-content-end">
  <li class="nav-item">
    <!-- funciona como: '/?locale=pt-BR' -->
    <%= link_to 'Português', root_path(locale: 'pt-BR'), class: "nav-link" %>
  </li>
  <li class="nav-item">
    <%= link_to 'English', root_path(locale: 'en'), class: "nav-link" %>
  </li>
</ul>

<div class="jumbotron">
  <h1 class="display-4"><%= t('welcome') %></h1>
  <p class="lead"><%= t('messages.welcome') %></p>
  <hr class="my-4">
  <p><%=t('messages.menu')%></p>
	<%= render 'menu' %>
</div>
